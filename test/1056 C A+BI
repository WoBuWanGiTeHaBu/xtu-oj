//思路：最难搞的是怎么滤过+=而且不停止；scanf普通字符会要求匹配，不按+=会出错


#include<stdio.h>
int main(){
	int n;
	scanf("%d",&n);
	while(n--){
		int a,b,c;
		scanf("%d+%d=%d",&a,&b,&c);
		if(a+b==c){
			printf("Yes\n");
		}else{
			printf("No\n");
		}
		
	}
	
	
	return 0;
}

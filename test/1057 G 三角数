//思路：观察可得，要这样写（bushi
//多写几行观察一下发现第n行有n个数。如果n是奇数
//{开头数字也是奇数，那么这一行的偶数就是(n-1)/2个。
//{开头数字是偶数，那么这一行的偶数就是(n+1)/2个。
//如果n是偶数，那么奇偶对半分。
//弄清楚这个规律，我们就可以先分n的奇偶，再靠开头数字来计算。
//n是偶数很好解决，用if分一下。下面讲奇数n。
//前面说了多写几行观察一下发现第n行有n个数，所以总共有多少个数可以用(n+1)*n/2来计算。
//但是因为我们要的是最开头的数，所以，我们用n-1来算上一行最后一个n*(n-1)/2。
//如果因为n是奇数，n-1必然是偶数，但是还要区分最后一个数的奇偶的话，/2之后还得再判断一次能不能被2整除。
//因为只判断n-1所以就不需要把n*(n-1)/2算出来，判断[(n-1)/2]/2能不能除尽就好/
//如果能，那么开头就是奇数，反则偶数。




#include<stdio.h>
int main(){
	int t;
	scanf("%d",&t);
	while(t--){
		long long n;
		scanf("%lld",&n);
		long long cnt=0;
		int q=n%2;
		if(q==0){
			cnt=n/2;
		}else{
			if((n-1)%4==0){
				cnt=(n-1)/2;
			}else{
				cnt=(n+1)/2;
			}
		}
		printf("%lld\n",cnt);	
	}	
	return 0;
}
